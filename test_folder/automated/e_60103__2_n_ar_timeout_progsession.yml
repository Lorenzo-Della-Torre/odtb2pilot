hvbm_p519:
  run:
      did: 'EDA0'
      number_of_frames: 1

ecm_phev:
  run:
      did: 'EDA0'
      number_of_frames: 1

ecm_hp:
  run:
      did: 'EDA0'
      number_of_frames: 1

ecm_lp:
  run:
      did: 'EDA0'
      number_of_frames: 1
